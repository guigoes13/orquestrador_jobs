# Cada seção deve corresponder a um nome registrado em orquestrador/registry.py.
# run_on_start=false evita alterar dados imediatamente ao iniciar o serviço.
[server]
host = 127.0.0.1
port = 5000

[job:processar_produtos]
enabled = true
interval_minutes = 15
run_on_start = false
